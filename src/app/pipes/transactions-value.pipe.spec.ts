import { TransactionsValuePipe } from './transactions-value.pipe';

describe('TransactionsValuePipe', () => {
  it('create an instance', () => {
    const pipe = new TransactionsValuePipe();
    expect(pipe).toBeTruthy();
  });
});
